///<reference types="Cypress"/>

import updatesItemsInCartClass from "../support/pom/updateCartItems";

const updateitem = new updatesItemsInCartClass();
describe("User can update items to cart",()=>{
    beforeEach(()=>{
    
    });
    it("User can increase or decrease the quantity of items already existed in cart",()=>{
    
    });
    it("User can add new items to cart",()=>{
    
    });
    })